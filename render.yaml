services:
  - type: web
    name: mexa-core
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt && cd backend && python -m scripts.crear_demo_formularios
    startCommand: cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT
